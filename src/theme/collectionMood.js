/**
 * --------------------------------------------------------
 * VYRA Fashion
 * Collection Mood
 *
 * Shared visual language for collection previews.
 *
 * Each collection carries a quiet emotional tone
 * before photography is introduced.
 *
 * These moods are intentionally subtle.
 *
 * Build with Heart.
 * --------------------------------------------------------
 */

const collectionMood = Object.freeze({

  weekend: {
    background:
      "linear-gradient(180deg, #F8F5F1 0%, #EEE6DD 100%)",
    accent: "#D8C8B8",
  },

  city: {
    background:
      "linear-gradient(180deg, #F5F3F0 0%, #E4DDD5 100%)",
    accent: "#B7A89A",
  },

  elegance: {
    background:
      "linear-gradient(180deg, #F4EFEA 0%, #DDD2C6 100%)",
    accent: "#AA8F79",
  },

});

export default collectionMood;